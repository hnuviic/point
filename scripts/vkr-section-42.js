/** Расширенный раздел 4.2 для главы 4 (≈ 2+ страницы) */
function flowChart(lines) {
  const C = require('./vkr-doc-common');
  return C.wireframe(lines);
}

function getSection42(T, F) {
  const C = require('./vkr-doc-common');

  return [
    C.subsectionHeading('4.2', 'Логика работы веб-приложения'),

    C.textParagraph(
      'Логика работы веб-приложения ВИСЗ-АНО-ТО строится по трёхуровневой схеме «клиент — сервер — данные». Клиентский уровень (браузер) отвечает за отображение HTML-страниц, отправку форм и подгрузку динамического контента через JavaScript. Серверный уровень (Node.js, Express, файл server.js) принимает HTTP-запросы, выполняет маршрутизацию, бизнес-логику и обращается к базе данных. Уровень данных (SQLite, файл data.sqlite) хранит заявки, учётные записи сотрудников и контент сайта. Описание в данном разделе согласовано со схемой архитектуры (приложение Л, файл architecture.html) и диаграммами деятельности (приложение Д).'
    ),

    C.textParagraph(
      'Точкой входа приложения является модуль server.js. При запуске команды npm start выполняется последовательность: загрузка переменных окружения (dotenv); открытие соединения с SQLite (better-sqlite3); выполнение initDb() — создание таблиц и миграций addColumnIfMissing; ensureAdminUser() — создание или обновление учётной записи администратора; подключение middleware (express.json, cors, express-session); регистрация маршрутов API, Admin и статики; запуск прослушивания порта PORT (по умолчанию 3000).'
    ),

    C.textParagraph(
      'Физическая организация исходного кода отражает модульный принцип разделения ответственности. В корневой директории проекта размещены точка входа server.js, манифест зависимостей package.json, файл базы данных data.sqlite и HTML-страницы публичной части (index.html, info.html, zap.html, seansy.html, podbor.html и др.). Шаблоны административной панели сосредоточены в каталоге views/ (admin-home.ejs, admin.ejs, admin-sessions.ejs, partials/admin-nav.ejs). Клиентские скрипты — в js/ (site-config.js, news-dynamic.js, sessions-dynamic.js, parent-quiz.js, ai-chat.js), стили админки — в admin/assets/, вспомогательная логика — в lib/platform.js. Такое расположение упрощает навигацию разработчика и сопровождение системы. Структура каталогов проекта в среде разработки представлена на рисунке 14.'
    ),
    C.figureCaption('Рисунок 14 – Структура каталогов проекта'),

    C.textParagraph(
      'Состав программных зависимостей, обеспечивающих работу HTTP-сервера, шаблонизатора, СУБД и механизмов аутентификации, зафиксирован в файле package.json (рисунок 15). В блоке dependencies указаны runtime-библиотеки: express (маршрутизация и middleware), ejs (рендеринг admin*.ejs), better-sqlite3 (доступ к data.sqlite), bcryptjs (хеширование паролей), express-session (сессии сотрудников), cors (кросс-доменные запросы к API), dotenv (переменные окружения из .env). Скрипт npm start запускает node server.js; в engines зафиксирована версия Node.js 20.x. Блок devDependencies (docx, mammoth, sharp) используется только при генерации пояснительной записки и на работу портала в production не влияет.'
    ),
    C.figureCaption('Рисунок 15 – Зависимости проекта'),

    C.textParagraph(
      'Иерархическая подчинённость модулей следующая. Корневой модуль server.js делегирует обработку запросов дочерним подсистемам: API Routes (программные интерфейсы для форм и чата), Admin Routes (административная панель на EJS), Static Routes (отдача HTML, CSS, JS, изображений). Каждая подсистема вызывает обработчики (handlers), которые читают или записывают данные в SQLite либо формируют HTML через шаблонизатор EJS. Параллельно на клиенте автономно работают модули news-dynamic.js, sessions-dynamic.js, quiz.js и ai-chat.js, обращающиеся к API без перезагрузки страницы.'
    ),

    C.textParagraph(
      'Информационно-сервисный портал АНО «Точка Открытий» реализован на основе модульной архитектуры, что позволяет разделить функциональность системы на отдельные взаимосвязанные компоненты. Архитектура веб-приложения представлена в приложении Л. Каждый модуль отвечает за выполнение определённого набора задач и взаимодействует с другими частями системы посредством серверной логики и базы данных. Такой подход обеспечивает удобство сопровождения программного продукта, возможность дальнейшего расширения функционала и повышение надёжности работы системы.'
    ),
    C.textParagraph(
      'Центральным элементом системы является база данных SQLite, в которой хранится информация о заявках, учётных записях сотрудников, новостях, событиях, расписании сеансов, результатах теста подбора курса и методических материалах. Все функциональные модули обращаются к базе данных для получения, изменения или сохранения информации.'
    ),
    C.textParagraph(
      'Первым этапом работы сотрудника с системой является прохождение процедуры авторизации. Для этого используется модуль авторизации (маршрут /admin/login в server.js): проверка логина и пароля через bcrypt, определение роли (admin, coordinator, specialist) и формирование сессии adminUser. При успешном входе выполняется переход на главную страницу административной панели (admin-home.ejs) — персонализированный дашборд со статистикой заявок, новостей, событий и материалов библиотеки.'
    ),
    C.textParagraph(
      'Обработка заявок на запись на приём и обращений через форму обратной связи осуществляется модулями Booking Handler и Contact Handler. Родитель или партнёр заполняет форму на zap.html или contact.html; после серверной валидации данные сохраняются в booking_requests или contact_requests со статусом «Новая» и становятся доступны координатору в /admin. Специалист видит только назначенные ему записи; координатор — все обращения; администратор — полный доступ.'
    ),
    C.textParagraph(
      'Публикация новостей и анонсов мероприятий реализована в admin-news.ejs и admin-events.ejs; расписание сеансов — в admin-sessions.ejs; результаты теста родителей — в admin-quiz.ejs. Опубликованный контент выдаётся через GET /api/content/* и отображается на публичных страницах скриптами news-dynamic.js и sessions-dynamic.js. Управление учётными записями сотрудников — admin-users.ejs (только role admin). Консультирование посетителей — ai-chat.js и серверный Chat Handler (локальные шаблоны или OpenAI API). Хранение и обработка данных — драйвер better-sqlite3.'
    ),
    C.textParagraph(
      'Публичная часть сайта (index.html, info.html, proect.html, news.html, contact.html, zap.html, seansy.html, podbor.html) отдаётся Static Handler; динамические фрагменты подгружаются с сервера через API. Структурная иерархия модулей (server.js → API / Admin / Static → handlers → SQLite / EJS) дополнительно представлена в приложении Л (architecture.html).'
    ),

    C.textParagraph(
      'Сводное описание программных модулей, их назначения и направлений обмена данными систематизировано в таблице 21. Для каждого компонента указаны входная и выходная информация, а также вызываемые подсистемы — это позволяет увидеть, как клиентские скрипты, серверные обработчики и СУБД связаны между собой.'
    ),

    ...T(
      'Модули веб-приложения и их взаимосвязи',
      ['Модуль', 'Назначение', 'Входная информация', 'Выходная информация', 'Вызываемые компоненты'],
      [
        ['server.js', 'Инициализация Express, маршрутизация', 'HTTP-запрос', 'HTTP-ответ', 'SQLite, EJS, fetch OpenAI'],
        ['Middleware', 'Разбор тела, CORS, сессии', 'Заголовки, cookies', 'req.session', 'express.json, cors, session'],
        ['Static Handler', 'Публичный сайт', 'GET /index.html и др.', 'HTML, CSS, JS', 'Файловая система'],
        ['Contact Handler', 'Форма «Контакты»', 'JSON: name, phone, …', '{ ok, id }', 'INSERT contact_requests'],
        ['Booking Handler', 'Форма «Запись»', 'JSON: name, phone, child…', '{ ok, id }', 'INSERT booking_requests'],
        ['Content API', 'Новости, события, сеансы', 'GET без тела', '{ items: [] }', 'SELECT site_news, site_events, therapy_sessions'],
        ['Quiz Handler', 'Тест podbor.html', 'POST answers_json', '{ recommendation }', 'INSERT parent_quiz_results'],
        ['Sessions Admin', 'Расписание сеансов', 'POST форма сеанса', 'redirect', 'INSERT therapy_sessions'],
        ['Chat Handler', 'Чат-помощник', '{ message, history }', '{ reply, source }', 'OpenAI API / chatLocalReply'],
        ['Auth Handler', 'Вход сотрудника', 'username, password', 'redirect, session', 'SELECT admin_users, bcrypt'],
        ['Admin Handler', 'Кабинет, заявки', 'Session, POST-действия', 'HTML admin*.ejs', 'SELECT/UPDATE заявок'],
        ['news-dynamic.js', 'Рендер на клиенте', 'GET /api/content/*', 'DOM-элементы', 'Браузер'],
        ['ai-chat.js', 'Виджет чата', 'POST /api/chat', 'Сообщения в панели', 'Браузер → server.js'],
      ]
    ),

    C.textParagraph(
      'Соответствие URL-адресов, HTTP-методов и программных обработчиков, зарегистрированных в server.js, приведено в таблице 22. Карта маршрутов охватывает группы API (формы, контент, чат, quiz), Admin (кабинет, заявки, сеансы, audit, export) и Static (публичные HTML/CSS/JS). Таблица используется при отладке REST-интерфейса и проверке разграничения доступа к закрытым разделам /admin/*.'
    ),

    ...T(
      'Карта маршрутов server.js',
      ['Группа', 'Маршрут', 'Метод', 'Обработчик / файл'],
      [
        ['API', '/api/health', 'GET', 'Проверка работоспособности'],
        ['API', '/api/contact', 'POST', 'Contact Handler'],
        ['API', '/api/booking', 'POST', 'Booking Handler'],
        ['API', '/api/chat', 'POST', 'Chat Handler'],
        ['API', '/api/content/news', 'GET', 'SELECT site_news'],
        ['API', '/api/content/events', 'GET', 'SELECT site_events'],
        ['API', '/api/content/sessions', 'GET', 'SELECT therapy_sessions'],
        ['API', '/api/quiz/questions', 'GET', 'Вопросы теста'],
        ['API', '/api/quiz/submit', 'POST', 'Quiz Handler'],
        ['Admin', '/admin/sessions', 'GET/POST', 'admin-sessions.ejs'],
        ['Admin', '/admin/quiz', 'GET', 'admin-quiz.ejs — результаты теста'],
        ['Admin', '/admin/audit', 'GET', 'admin-audit.ejs (только admin)'],
        ['Admin', '/admin/export/requests', 'GET', 'CSV экспорт заявок'],
        ['Admin', '/admin/login', 'GET/POST', 'admin-login.ejs, bcrypt'],
        ['Admin', '/admin/home', 'GET', 'admin-home.ejs'],
        ['Admin', '/admin', 'GET', 'admin.ejs — список заявок'],
        ['Admin', '/admin/requests/…/status', 'POST', 'Смена status заявки'],
        ['Admin', '/admin/news, /events, …', 'GET/POST', 'Управление контентом'],
        ['Static', '/, /index.html, …', 'GET', 'express.static'],
      ]
    ),

    C.textParagraph(
      'Таблицы 21 и 22 дополняют графическую схему приложения Л: первая фиксирует логические связи модулей, вторая — техническую привязку запросов к обработчикам. Ниже описан укрупнённый алгоритм обработки произвольного HTTP-запроса и пошаговая логика работы отдельных модулей.'
    ),

    C.textParagraph(
      'Укрупнённый алгоритм обработки произвольного запроса пользователя: (1) браузер формирует HTTP-запрос к серверу; (2) Express определяет маршрут по URL и методу; (3) для защищённых путей /admin/* вызывается middleware requireAdmin — при отсутствии сессии редирект на /admin/login; (4) обработчик выполняет операцию (чтение файла, INSERT/SELECT в БД, render EJS); (5) формируется ответ (HTML, JSON или redirect); (6) клиент отображает результат (страница, alert, обновление DOM). Для запросов к /api/* и статике сессия не обязательна.'
    ),

    C.textParagraph(
      'Далее последовательно рассмотрены алгоритмы работы основных модулей системы — от приёма заявок на публичной части сайта до управления контентом и учётными записями в административной панели. Для каждого модуля приведено словесное описание входных и выходных данных, шагов обработки на клиенте и сервере, а также роли человека в процессе. Блок-схемы алгоритмов размещены на рисунках настоящего раздела; расширенные версии — в приложении Д (flowchart-booking.html, flowchart-content-admin.html, flowchart-users-admin.html, flowchart-requests.html, flowchart-admin-auth.html, flowchart-ai-chat.html).'
    ),

    C.textParagraph('Модуль «Запись на приём» (zap.html, Booking Handler)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — приём заявок на диагностику и консультацию с сохранением в единой базе. Вход клиента: страница zap.html, форма #bookingForm, поля name, phone (обязательные), child_name, direction, desired_date, comment, consent (checkbox). Алгоритм клиента: (1) событие submit, e.preventDefault(); (2) сбор payload, consent = 1 при отмеченном checkbox; (3) fetch POST /api/booking (URL из site-config.js); (4) при resp.ok — скрытие #bookingFormContainer, отображение #successMessage; (5) при ошибке — alert, восстановление кнопки «Отправить заявку». Алгоритм сервера (app.post /api/booking): (1) деструктуризация req.body; (2) если !name || !phone → 400 «Поля name и phone обязательны»; (3) формирование record (trim строк, status = new, processed = 0, created_at, ip, user_agent); (4) если consent !== 1 → 400 «Нужно подтвердить согласие…»; (5) prepared INSERT в booking_requests; (6) ответ { ok: true, id: lastInsertRowid }. Выход: запись в SQLite и подтверждение пользователю. Человек: родитель заполняет форму; координатор обрабатывает заявку в /admin (assign, смена status). Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ Открытие zap.html           │',
        '│ форма #bookingForm          │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ Ввод: name, phone, child,   │',
        '│ direction, date, comment,   │',
        '│ consent (checkbox)          │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ submit → preventDefault       │',
        '│ fetch POST /api/booking     │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ Сервер: name и phone есть?  │──────────► 400',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ consent = 1?                │──────────► 400 согласие ПДн',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ INSERT booking_requests     │',
        '│ status=new, processed=0     │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ { ok: true, id }            │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ #successMessage на zap.html │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ /admin — координатор (чел.) │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм работы модуля «Запись на приём»'
    ),

    C.textParagraph('Модуль «Управление контентом» (admin-news, events, reports, library)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — создание, публикация и снятие с публикации материалов сайта сотрудниками центра. Вход: сессия admin/coordinator (requireAdmin, requireRole), маршруты GET/POST /admin/news, /admin/events, /admin/reports; библиотека — /admin/library (specialist добавляет материалы со status=pending, координатор — POST …/approve). Алгоритм создания (на примере новостей): (1) GET /admin/news — SELECT site_news с JOIN author_id, render admin-news.ejs; (2) POST форма: title, body, category, published; (3) если !title || !body → 400, render с ошибкой; (4) INSERT в site_news (author_id = id сотрудника, timestamps); (5) redirect /admin/news; (6) POST /admin/news/:id/toggle — инверсия published; (7) при published=1 записи попадают в GET /api/content/news и отображаются через news-dynamic.js на index.html и news.html. Аналогично для site_events, site_reports. Выход: HTML-кабинет и обновлённый публичный контент. Человек: координатор или admin заполняет формы. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ /admin (сессия, requireAdmin)│',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ role admin или coordinator? │──────────► 403',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ GET /admin/news|events|…    │',
        '│ SELECT + render admin-*.ejs │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ title, body заполнены?      │──────────► 400',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ INSERT site_news / events…  │',
        '│ author_id, published        │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ toggle published (опц.)     │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ GET /api/content/* → сайт   │',
        '│ news-dynamic.js             │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм работы модуля «Управление контентом»'
    ),

    C.textParagraph('Модуль статической раздачи (Static Handler)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — доставка публичных HTML-страниц, таблиц стилей, скриптов и изображений без участия СУБД. Вход: GET-запрос с путём вида /index.html, /style.css, /js/ai-chat.js. Алгоритм: (1) запрос поступает в express.static после проверки, что путь не относится к /api и не перехвачен маршрутом /admin; (2) файловая система ищет файл относительно корня проекта; (3) при наличии файла — ответ 200 с Content-Type по расширению; (4) при отсутствии — 404. Выход: содержимое файла в теле ответа. Вызывается из: навигационных ссылок всех страниц, прямого ввода URL. Человек: просматривает страницу в браузере.'
    ),

    C.textParagraph('Модуль обратной связи (Contact Handler)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — регистрация обращений с формы contact.html. Вход: JSON { name, phone, email?, topic?, message?, consent }. Алгоритм аналогичен модулю записи (валидация name, phone, consent; INSERT в contact_requests; ответ { ok, id }), но клиент при успехе показывает alert и сбрасывает форму. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ contact.html — форма        │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ consent = 1 и name, phone?  │──────────► 400 Ошибка',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ POST /api/contact (JSON)    │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ INSERT contact_requests     │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ { ok: true, id } → alert    │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм обработки обращения (contact.html)'
    ),

    C.textParagraph(
      'Публичная выдача контента (GET /api/content/*, news-dynamic.js) продолжает логику модуля управления контентом: на клиенте при DOMContentLoaded выполняются loadEvents() и loadNews(), данные с published = 1 рендерятся в контейнеры index.html и news.html.'
    ),

    C.textParagraph('Модуль «Управление пользователями» (admin-users)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — учёт сотрудников центра с ролевым доступом. Вход: только role = admin, маршруты GET/POST /admin/users. Алгоритм просмотра: SELECT id, username, role, full_name, created_at FROM admin_users; render admin-users.ejs. Алгоритм создания: (1) POST: username, password, role, full_name; (2) проверка непустых username, password и role ∈ {admin, coordinator, specialist} — иначе 400; (3) password_hash = bcrypt.hashSync(password, 10); (4) INSERT admin_users; (5) при дубликате username (UNIQUE) — 400 «Пользователь с таким логином уже существует»; (6) redirect /admin/users. Выход: новая учётная запись для входа через /admin/login. Человек: администратор центра. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ session.role = admin?       │──────────► 403',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ GET /admin/users            │',
        '│ SELECT admin_users          │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ login, password, role OK?   │──────────► 400',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ bcrypt.hashSync → INSERT    │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ username уникален?          │──────────► 400 дубликат',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ redirect /admin/users       │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм работы модуля «Управление пользователями»'
    ),

    C.textParagraph('Модуль административной аутентификации (Auth Handler)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — идентификация сотрудников центра. Вход: POST /admin/login, поля username, password. Алгоритм: (1) SELECT * FROM admin_users WHERE username = ?; (2) если запись не найдена — 401, render admin-login с ошибкой «Неверные данные входа»; (3) bcrypt.compareSync(password, password_hash); (4) при неверном пароле — 401; (5) при успехе — req.session.adminUser = { id, username, role, full_name }; (6) redirect на /admin/home. Middleware requireAdmin на последующих запросах проверяет req.session.adminUser; requireRole ограничивает доступ по роли (admin, coordinator, specialist). Выход: сессионная cookie, HTML кабинета. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ GET /admin/login — форма    │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ POST: username, password    │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ Пользователь в admin_users? │──────────► 401 Неверные данные',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐     НЕТ',
        '│ bcrypt.compareSync OK?      │──────────► 401 Неверные данные',
        '└──────┬──────────────────────┘',
        '       │ ДА',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ session.adminUser = {…}     │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ redirect /admin/home        │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм административной аутентификации'
    ),

    C.textParagraph('Модуль обработки заявок в кабинете (Admin Handler — заявки)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — просмотр и маршрутизация заявок сотрудниками. Вход: GET /admin (сессия), POST /admin/requests/:kind/:id/status, POST assign, POST note. Алгоритм просмотра: (1) requireAdmin; (2) если role = specialist — SELECT booking_requests WHERE assigned_to = id сотрудника; иначе — SELECT всех contact_requests и booking_requests с JOIN admin_users для ФИО ответственного; (3) объединение списков, сортировка по created_at; (4) render admin.ejs. Алгоритм смены статуса: (1) normalizeStatus текущей записи; (2) nextStatus: new → in_progress → done → new; (3) UPDATE status и processed; (4) redirect /admin. Назначение специалиста (только coordinator/admin): UPDATE booking_requests SET assigned_to = ?. Человек: координатор звонит клиенту, меняет статус; специалист добавляет specialist_note. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        'GET /admin (сессия есть)',
        '       ▼',
        'SELECT contact_requests + booking_requests',
        '       ▼',
        'Отображение таблицы заявок (admin.ejs)',
        '       ▼',
        '┌──────────────────────────────────────┐',
        '│ Сотрудник: «Изменить статус»         │',
        '└──────┬───────────────────────────────┘',
        '       ▼',
        'POST /admin/requests/{contact|booking}/{id}/status',
        '       ▼',
        'nextStatus: new → in_progress → done',
        '       ▼',
        'UPDATE в SQLite, redirect /admin',
        '       ▼',
        'Опционально: assign специалиста (booking)',
      ],
      'Алгоритм обработки заявки в административной панели'
    ),

    C.textParagraph('Модуль чат-помощника (Chat Handler и ai-chat.js)', { bold: true, noIndent: true, after: 120 }),
    C.textParagraph(
      'Назначение — автоматические ответы на типовые вопросы посетителей. Вход: POST /api/chat, { message, history? }. Алгоритм клиента: (1) открытие виджета — приветствие WELCOME; (2) ввод текста или быстрая кнопка (QUICK); (3) fetch POST с JSON; (4) отображение «Печатаю…»; (5) вывод reply в панель; (6) history до 20 сообщений. Алгоритм сервера: (1) trim message, проверка непустоты — иначе 400; (2) если OPENAI_API_KEY задан — fetch api.openai.com, model gpt-4o-mini, system prompt CHAT_SYSTEM, history до 8 реплик; (3) при успехе — { reply, source: "openai" }; (4) при отсутствии ключа или ошибке — chatLocalReply (regex по ключевым словам); (5) source: "local". Человек: читает ответ; при сложном вопросе переходит на contact/zap. Алгоритм представлен на рисунке ниже.'
    ),
    ...F(
      [
        '┌─────────────┐',
        '│   НАЧАЛО    │',
        '└──────┬──────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ Открытие виджета ai-chat.js │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ Ввод сообщения пользователем│',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ POST /api/chat              │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐  ДА',
        '│ OPENAI_API_KEY в .env?      │────► OpenAI gpt-4o-mini',
        '└──────┬──────────────────────┘              │',
        '       │ НЕТ                                 │',
        '       ▼                                     │',
        '┌─────────────────────────────┐              │',
        '│ chatLocalReply (шаблоны)    │◄─────────────┘',
        '└──────┬──────────────────────┘   (при ошибке API)',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ JSON { reply, source }      │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────────────────────┐',
        '│ Отображение ответа в чате   │',
        '└──────┬──────────────────────┘',
        '       ▼',
        '┌─────────────┐',
        '│    КОНЕЦ    │',
        '└─────────────┘',
      ],
      'Алгоритм работы чат-помощника'
    ),

    C.textParagraph(
      'При описании алгоритмов модулей необходимо разграничить операции, выполняемые программной частью автоматически, и действия, которые остаются за посетителями сайта и сотрудниками центра. Автоматизация обеспечивает приём и хранение заявок, валидацию данных, выдачу контента и ответы чат-помощника; решение по обращению, звонок семье, назначение специалиста и публикация материалов инициируются человеком через административную панель. Такое распределение отражает социально ориентированный характер портала: система снимает рутину, а персональный контакт с семьями сохраняется. Сводное соотношение функций системы и участников процесса приведено в таблице 23.'
    ),

    ...T(
      'Распределение операций между системой и человеком',
      ['Операция', 'Выполняет система', 'Выполняет человек'],
      [
        ['Заполнение формы на сайте', '—', 'Родитель / посетитель'],
        ['Валидация и INSERT заявки', 'server.js', '—'],
        ['Просмотр списка заявок', 'admin.ejs', 'Координатор / admin'],
        ['Звонок клиенту', '—', 'Координатор'],
        ['Смена status, assign', 'UPDATE SQLite', 'Инициирует человек'],
        ['Публикация новости', 'INSERT site_news, toggle published', 'Координатор / admin'],
        ['Создание учётной записи', 'INSERT admin_users, bcrypt', 'Администратор (admin)'],
        ['Ответ чат-помощника', 'Chat Handler', '—'],
        ['Вход в admin', 'bcrypt, session', 'Вводит логин/пароль'],
      ]
    ),

    C.textParagraph(
      'Таким образом, описанная логика охватывает полный цикл работы веб-приложения: от запуска server.js и маршрутизации запросов до обработки заявок, публикации контента и взаимодействия с посетителями через чат-помощника. Иерархия модулей приведена в приложении Л (architecture.html); блок-схемы алгоритмов — на рисунках настоящего раздела и в приложении Д.'
    ),
  ];
}

module.exports = { getSection42 };
